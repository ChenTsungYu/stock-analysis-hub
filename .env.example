# Local-only credentials. Copy this file to .env; never commit the real file.
# SUBSTACK_SID takes priority when set. Otherwise set both email and password.
SUBSTACK_SID=
SUBSTACK_EMAIL=
SUBSTACK_PASSWORD=
